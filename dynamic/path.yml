http:
  # Define URL routing rules, middlewares, and services
  routers:
    nginx-frontend:
      rule: "Host(`localhost`)" # Match requests to localhost without /api prefix
      entryPoints:
        - web # Listen on the web (HTTP) entry point
      service: nginx # Forward to the nginx service

    backend-api-device:
      rule: "Host(`localhost`) && PathPrefix(`/api_device`)" # Match requests to localhost with /api prefix
      entryPoints:
        - web # Listen on the web (HTTP) entry point
      middlewares:
        - auth-basic # Apply basic auth middleware
        - strip-api-prefix-device # Remove /api prefix before forwarding
      service: api-service-device

  middlewares:
    strip-api-prefix-device: # Middleware to remove /api prefix
      stripPrefix: 
        prefixes:
          - "/api_device"

    auth-basic: # Middleware for basic authentication
      basicAuth:
        users:
          - "user:$apr1$H6uskkkW$IgXLP6ewTrSuBkTrqE8wj/" # password = "test"

  services:
    nginx: 
      loadBalancer:
        servers:
          - url: "http://nginx:80" # Container name solved by Docker DNS

    api-service-device:
      loadBalancer:
        servers:
          - url: "http://container_device:8080" # Container name solved by Docker DNS
